node('slave-rte-math-prog-linux'){
    // // TODO à rendre dynamique
    // def versionPNE = 'PNE_XPRESS'
    // def versionLivrable = '1.0'
    // def os = 'linux'
    // def fichier = 'PNE_STANDALONE'
    

    // // Récupération des sources
    // stage('Checkout'){
    //     checkout scm
    // }

    // // Compilation
    // stage('Compilation'){
    //     sh """
    //     cmake ./src
    //     make
    //     """
    // }

    // // Tests
    // stage('Test'){
    //     sh """
    //     echo "test"
    //     """
    // }

    // // Creation d'un tar gz pour livraison
    // stage('Archive'){
    //     sh "tar cvf PNE_STANDALONE.tar.gz libbib_solveur.a PNE_STANDALONE"
    // }

    // // Publication dans Nexus
    // stage ('Publication'){
	//     nexusArtifactUploader artifacts: [[artifactId: versionPNE, classifier: os, file: fichier, type: 'tar.gz']], credentialsId: 'jenkins-nexus-pne', nexusUrl: 'devin-depot.rte-france.com', nexusVersion: 'nexus3', protocol: 'https', repository: 'pne', version: versionLivrable
    // }
}
